build a convex based website that uses github for authentication where people can upload skills skills are multiple files (no binaries). We want a vector-based search over it and comments and an API to download them. they should be stored on convex as well.
Simple minimalistic design. We will parse skill metadata. Agent Skills is an open spec: https://agentskills.io/home however for clawdis we extended the spec a bit, see ../clawdis for our skill additions. Users can public multiple skills and update skills so we want to track versions and have a change view/changelog of them. Later we build a cli that allows to download/upload/sync skills, the cli authenticates to clawdhub.com somewhat like the npmjs ecosystem. There is no review but skills will have badges for "redaction approved" and people can filter in search to only see skills that we recommend/have approved. Comments should be able to be deleted by either the user itself or by moderators, so users can have levels: admin, moderator, user. A user can start skills they like for a faster view. ask me questions. use latest dependencies. write this in modern strict typescript with biome + oxlint typeaware as linter, we use vitest setting menu for Name and to delete account. Avatar we get from ideally github or gravatar as fallback.
